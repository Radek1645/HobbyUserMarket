# Stav projektu — 2026-09-01 (po půlnoci)

**Čas zápisu:** ~00:10  
**PRD:** v3.92

Předchozí: [`2026-08-30.md`](./2026-08-30.md).

## Hotovo v této iteraci

| Oblast | Stav |
|--------|------|
| Soukromá událost (`is_private`) | ✅ kód + **082 na produkci** (uživatel). Badge fialový, `noindex`. Filtr ve výpisech, **ne** v `is_post_publicly_visible` |
| Vícedenní akce (`event_end_date`) | ✅ checkbox **Vícedenní akce**; jiný kalendářní den Praha; `expires_at` z konce |
| Sdílení | ✅ kolečko přes roh fotky; dialog kopírovat + native share + QR + stáhnout PNG |
| Hydratace „Vstup zdarma“ | ✅ nepropisuje se do textu, pokud to inzerent nenapsal. Edge `moderate-listing` **nasazeno** |
| Právní retence `081` | ✅ v kódu (cron, `hidden_at`, kredity archived). **SQL 081 v této session nepotvrzeno** |

## Infrastruktura

| Položka | Stav |
|---------|------|
| SQL `082_posts_private_events.sql` | ✅ produkce (uživatel) |
| SQL `081_legal_retention_and_hidden_at.sql` | ⚠️ **TODO ověřit** — v commitu je, nasazení uživatel u 082 nepotvrdil |
| Edge `moderate-listing` (Vstup zdarma + sync promptů) | ✅ `npx supabase functions deploy moderate-listing` (tato session) |
| `npm run sync:moderation` + `sync-build-prompt.mjs` | ✅ před deployem |
| Nové Vercel/Resend env | — není třeba |
| GTM | stávající `DETAIL_SHARE_LISTING`; nové ID nebylo |

Fingerprint / publish gate: `is_private` a `event_end_date` **nejsou** ve fingerprintu.

## Manuální testy

| Scénář | Výsledek |
|--------|--------|
| Soukromá oslava — `noindex, nofollow` (SEO extension) | ✅ uživatel |
| Badge **Soukromá událost** (majitel, pod H1) | ✅ po zvýraznění fialová |
| QR stáhnout PNG | ✅ tlačítko **Stáhnout QR** |
| Sdílení overlay na fotce | kód; E2E po Vercel |
| „Vstup zdarma“ mimo text | kód + Edge; u už publikované oslavy znovu uložit |

Prohlížeč v této uzavírací session agentem neprošel (žádné browser tools).

## Commity

- (doplní se po `git commit`)

## Mimo tento commit

- `docs/fb-ads/posty/`, cover PNG, `docs/naklady-mesicni.md`, `docs/vibe_engineering_blocks.md` — nenecháno do gitu (stejně jako 30. 8.).

## Na další session

- Ověřit, zda **081** běží na produkční DB; jinak cron `purge-legal-retention` padá
- Smoke sdílení overlay + QR download na produkci po Vercel Ready
- Smoke soukromá událost: není na HP, detail podle slugu ano
- Už publikovaná oslava: znovu uložit, ať zmizí „Vstup je zdarma“ z textu
