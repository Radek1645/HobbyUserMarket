# Stav projektu — 2026-08-25 (večer, částečný snapshot)

**Čas zápisu:** ~23:40  
**PRD:** v3.81

## Hotovo v této iteraci

| Oblast | Stav |
|--------|------|
| Prefill OpenAI fallback (Gemini primary → OpenAI `gpt-5.4-nano`) | ✅ kód |
| Resolver `resolve-suggest-ai-target.ts` + `runSuggestListingWithFallback` | ✅ |
| Časový rozpočet: request 28 s, Gemini max 12 s, OpenAI max 8 s | ✅ |
| Odstraněno mapování `GEMINI_BLOCKED_*` → NSFW u prefillu | ✅ |
| Audit `used_fallback` v `moderation_checks` + Edge log | ✅ |
| Docs: Metodika §5/§6, PRD v3.81, moderace, TO-DO §N, rule `ai-moderation.mdc` | ✅ |

## Infrastruktura

| Položka | Stav |
|---------|------|
| Nové SQL migrace | — žádné |
| `npm run sync:moderation` | — nepotřeba (prompt/taxonomie beze změny) |
| Edge `suggest-listing-from-photos` | ✅ deploy v16+ (uživatel potvrdil) |
| Secret `SUGGEST_LISTING_MODEL` | ✅ vrácen na `gemini-3.5-flash-lite` |
| Secret `SUGGEST_FALLBACK_MODEL` | — volitelný; default `gpt-5.4-nano` v kódu |
| Vercel / Next.js | ☐ push → auto deploy (tento snapshot) |

## Manuální testy (localhost → produkční Supabase Edge)

| Scénář | Výsledek |
|--------|----------|
| Gemini happy path (holínky) | ✅ `used_fallback=false`, ~1,6–1,9 s |
| Fallback (invalid `SUGGEST_LISTING_MODEL`) | ✅ OpenAI 2×, `used_fallback=true`, OpenAI dashboard 2 requesty |
| Návrat primary secretu | ✅ Gemini znovu OK |
| NSFW (Sightengine) | ✅ `moderation_checks`: `NSFW_IMAGE`, bez LLM |
| Guest rate limit po smoke | ✅ očekávaná hláška |

**Nebylo:** double-fail (`SUGGEST_BOTH_PROVIDERS_FAILED`), OpenAI-as-primary bez Gemini klíče.

## Commity (po pushi doplnit hash)

- `feat: OpenAI fallback for photo-first prefill` — Edge + docs (tento push)

## Na další session (dnes večer / zítra)

- Pokračovat v další práci (uživatel plánuje ještě dnes)
- Volitelně: double-fail smoke, `console.log` u NSFW reject v Edge pro snazší debugging
- FB / právní docs v working tree necommitovat s prefill commitem (samostatná větev)
