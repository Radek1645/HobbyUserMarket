# Stav projektu — 2026-07-21 (noc)

**Čas:** ~23:55  
**Větev:** `main`  
**Zaměření:** Hard stop — blacklist podle e-mailu + SoR e-maily + unban s obnovou inzerátů

---

## Hotovo

- Migrace **055** `account_blacklist` (+ RPC `is_email_blacklisted`, důvod `account_blacklist` u posts)
- Auto hard stop po **3 hard rejectech / 24 h** (Edge) → blacklist + skrytí `active`/`hidden`
- Middleware gate → `/ucet-pozastaven`
- Admin UI `/mod/blacklist` (add / soft unban + historie)
- Unban obnoví inzeráty s `status_reason_code = account_blacklist` na `active`
- E-maily: hard stop + unban (Resend); Edge webhook `POST /api/internal/notify-account-hard-stop`
- Cron `purge-hard-stop-evidence` (730 dní)
- Docs: Metodika §6.4, PRD v3.41, GDPR retence řádek, cursor-prompt fáze 2

## Infrastruktura (dle uživatele)

| Položka | Stav |
|---------|------|
| Migrace `055` | ✅ nasazeno |
| Edge `moderate-listing` | ✅ deploy |
| Edge secrets `CRON_SECRET` + `SITE_URL` | ✅ |
| Vercel `CRON_SECRET` (stejná hodnota) | ✅ (rotace / nastavení) |

## Manuální testy

- Smoke hard stop **zítra** — viz [`docs/TO-DO-dalsi-den.md`](../docs/TO-DO-dalsi-den.md) §1 (H1–H7)

## Backlog / Na další session

- Smoke H1–H7 + zbytky T/V/047 z checklistu
- Sightengine DPA + revize právníkem (VOP/CSAM/GDPR)
- Admin UI nad evidence fotkami (teď SQL)
- PhotoDNA / hash CSAM (mimo scope)
- Aktualizovat `docs/pravni/README.md` (stále „auto-suspend fáze 2“)

## Commity

- `b135447` feat: hard stop blacklist podle e-mailu (055) a SoR e-maily
