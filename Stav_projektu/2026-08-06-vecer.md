# Stav projektu — 2026-08-06 (večer)

**Čas zápisu:** odpoledne / ukončení session Category SEO  
**PRD:** v3.60 · CATEGORY_SEO v1.1 · WAVE1

## Hotovo v této session

| Oblast | Stav |
|--------|------|
| CSEO1–3 (plán) | ✅ 1A goods-only, slug 1:1 config, hystereze 3/14, práh lokalita ≥5 odloženo |
| Migrace `072_category_seo_pages` | ✅ SQL + seed 8 Wave1 slugů; **apply na Supabase ověřen** (localhost čte copy z DB) |
| Route `/{slug}` | ✅ `src/app/[slug]/page.tsx` — unikátní goods slugy; jinak 404 |
| Cron + sitemap | ✅ `/api/cron/category-seo-index` + `vercel.json` 05:00 UTC; sitemap jen `index_status=index` |
| Smoke `/kola-kolobezky` | ✅ 3 inzeráty, title/desc/canonical; po SQL backdate + cron → `index, follow` + v sitemapě |
| Docs | ✅ PRD v3.60, Metodika §8.5, supabase-prikazy schéma, terminal-prikazy (cron PowerShell), TO-DO § H |

## Infrastruktura

| Položka | Stav |
|---------|------|
| SQL `072` | ✅ nasazeno (uživatel — stránka funguje proti DB) |
| Edge Functions | N/A (bez změny moderace) |
| `CRON_SECRET` | ✅ existující (stejný jako ostatní cróny); nový cron path na Vercel po deployi |
| Vercel build po pushi | TODO ověřit zelený build + produkční `/kola-kolobezky` |

## Manuální testy

- `/kola-kolobezky` — H1, 3 karty, breadcrumb
- Meta `noindex` před hysterezí → po `UPDATE above_threshold_since` + cron → `index, follow`
- Sitemap konzistentní s `index_status` (ne s raw `listing_count`)

## Na další session

- Produkční smoke po Vercel deployi
- Interní crawlovatelné odkazy HP → kategorie (zatím sitemap + přímá URL)
- I6 negativní security smoke fotek
- Vlna 2 lokalita až po objemu (≥5)

## Commity

- `dd9d20f` — feat: Category SEO Vlna 1 — landings /{slug}/, cron a index_status
