# Stav projektu — 2026-07-10

> **Čas:** večer  
> **PRD:** v3.21  
> **Branch:** `main` (commity po této session)

## Co je hotové

### Stav `blocked` (moderace inzerátů)
- Nový `posts.status = 'blocked'` — oddělení od dobrovolné pauzy (`hidden`)
- Sloupec `status_reason_code`: `reports_threshold` | `moderation`
- Migrace **`036_post_status_blocked.sql`** — trigger 3× nahlášení → `blocked`; publish gate; revert fotek
- UI: badge „Zablokováno“, `ListingBlockedNotice`, akce jen Upravit + Smazat
- Obnovení: úprava obsahu/fotek → `draft` → AI → `publish_approved_post`
- Konfigurace textů: `src/config/listing-status-reasons.ts`

### Právní dokumentace (draft)
- `docs/pravni/` — VOP, GDPR, cookies, DSA centrum, pravidla inzerce
- § o zablokování inzerátu vs. pozastavení majitelem

### Inquiry + expirace (související necommitnutá práce v repu)
- Migrace **`033`–`035`**: `inquiry_events`, `inquiry_no`, RPC `archive_expired_posts`
- Rate limit a validace poptávky (`/api/inquiry`)
- Vercel cron `archive-expired` (denně 03:00) + lazy archivace v `/moje-inzeraty`
- `listing-status.ts` — expirovaný `active` zobrazit jako „Expirovaný“

### AI moderace — hardening
- Prompt injection guard (klient + Edge Function)
- `bound-user-content`, rozšířený `prohibited-scan` v EF
- Sync skript + aktualizované prompty

### UX / drobnosti
- Onboarding, nickname validace, inquiry formulář, Site Notice úpravy
- `docs/ukazka-moderace-hydratace.md` (ukázka)

## Nasazení infrastruktury

| Oblast | Stav |
|--------|------|
| Supabase **036** (`blocked`) | ✅ nasazeno (potvrzeno uživatelem) |
| Supabase **033–035** | ⏳ v repu — ověřit v SQL editoru |
| Edge Functions | ⏳ redeploy po `sync:moderation`, pokud se měnily prompty |
| Vercel `CRON_SECRET` | ⏳ nastavit pro `/api/cron/archive-expired` |

## Manuální testy

| Scénář | Výsledek |
|--------|----------|
| Migrace 036 v Supabase | ✅ (uživatel) |
| UI `/moje-inzeraty` — badge Zablokováno + notice | ⏳ po deploy |
| 3× nahlášení → `blocked` (ne `hidden`) | ⏳ |
| Edit blocked → draft → re-moderace | ⏳ |
| Pauza `hidden` → Zveřejnit bez AI | ⏳ regrese |
| Cron archivace expirovaných | ⏳ |
| Build `npm run build` | ⏳ |

## Commity (plán)

1. `feat: blocked status, inquiry log, archive cron` — kód + migrace 033–036
2. `docs: PRD v3.21, právní drafty, Stav 2026-07-10` — dokumentace

## Na další session

- God Mode UI pro ruční `blocked` + Statement of Reasons e-mail
- Ověřit nasazení migrací 033–035
- Manuální test 3× report → `blocked`
- Redeploy `moderate-listing` po syncu promptů
- Nastavit `CRON_SECRET` na Vercelu
