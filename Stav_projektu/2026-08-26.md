# Stav projektu — 2026-08-26

**Čas zápisu:** ~23:55  
**PRD:** v3.82

## Hotovo v této iteraci

| Oblast | Stav |
|--------|------|
| Landing `/prodejte-snadno` (FB kampaň Brno) | ✅ kód |
| Meta Pixel `1774699993535627` v appce (ne GTM) | ✅ |
| Eventy: PageView SPA, ViewContent, InitiateCheckout, Lead, CompleteRegistration | ✅ |
| UTM / fbclid v `localStorage` (`zapikolou:campaign_query`) + `next` u guest loginu | ✅ |
| Consent: Pixel až po marketingu; Jen analytika → `fbq` undefined | ✅ ověřeno localhost |
| Kuchařka konzole [`docs/fb-ads/MERENI-console.md`](../docs/fb-ads/MERENI-console.md) | ✅ |
| Doména `zapikolou.cz` ověřená v Metě (TXT Cloudflare) | ✅ uživatel |

## Infrastruktura

| Položka | Stav |
|---------|------|
| Nové SQL migrace | — žádné (uživatel) |
| Edge Functions deploy | — není třeba (uživatel) |
| Vercel env | ✅ defaulty v kódu; push na `main` nasadí web |
| DNS TXT Meta | ✅ Cloudflare `@` `facebook-domain-verification=m2oqxp2xbsthv8soz29qzz3x0ohsib` |
| Meta Verify domain | ✅ zelená (uživatel) |

## Manuální testy (localhost, anonymní okno)

| Scénář | Výsledek |
|--------|----------|
| Před souhlasem `typeof fbq` undefined; po Přijmout vše `2.9.385` | ✅ |
| Jen analytika: `analytics_storage granted`, `ad_* denied`, `fbq` undefined | ✅ |
| SPA PageView (`/inzerat/novy`, HP, filtr kategorie) | ✅ |
| InitiateCheckout jednou za tab | ✅ |
| UTM v URL → `zapikolou:campaign_query` po publikaci | ✅ |
| Guest login: UTM v `next`, po návratu v URL i Lead `cd[utm_*]` | ✅ |
| `ev=Lead` právě jednou (ostatní TR = PageView) | ✅ |
| `cd[content_category]=detsky` | ✅ |

**Nebylo na produkci:** Pixel Helper / Test Events na `zapikolou.cz` (až po Vercel deployi).

## Commity

- (hash po pushi) — feat: FB landing, Meta Pixel Lead + UTM, ověření domény

## Na další session

- Po Vercel deployi: Pixel Helper + Test Events na produkci (`/prodejte-snadno` s UTM → publikace → Lead 1×)
- `CompleteRegistration` u úplně nového účtu (localhost E3 v TO-DO zatím neověřeno)
- Spustit FB kampaň až po produkčním Lead v Events Manageru
