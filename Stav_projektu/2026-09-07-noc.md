# Stav projektu — 2026-09-07 (noc)

**Čas zápisu:** ~21:30  
**PRD:** v3.100

Předchozí: [`2026-09-07-vecer.md`](./2026-09-07-vecer.md) (~20:25, v3.99).

## Hotovo v této iteraci

### Prefill popis v `moderation_checks`

Sloupec `suggest_description` (migrace **084**, max 2000, jen `intent = suggest_from_photos`). Úspěšný Prefill ho zapisuje; NSFW / výpadek Sightengine a create/update ne. Denní SQL H1/H2 v Metodice §6.12.

### GDPR

Živé FO **1.9-fo** / OSVČ **1.3-osvc**: nový řádek tabulky *Kontrola obsahu inzerátu* (12 měsíců) + věta v §3.3 pro hosta. `CURRENT_GDPR_VERSION` sync. Draft 2.2-osvc v `docs/pravni/navrh-claude/` (gitignore).

### Vedle (jde ve stejném commitu)

- `/moje-inzeraty`: název aktivního inzerátu odkazuje na veřejný detail
- Prefill lab hydratace: fixture bez fixní ceny (dohodou) — 100 Kč u DualSense shazovalo `scam_fraud`
- Skill `ukonceni-prace`: default commit = celý working tree

## Infrastruktura

| Položka | Stav |
|---------|------|
| SQL **084** | ✅ nasazeno (ověřeno na produkci: sloupec + CHECK, bez grantu anon/authenticated) |
| Edge `suggest-listing-from-photos` | ✅ nasazeno v této session (včetně `log-moderation-check.ts`) |
| `moderate-listing` | beze změny, redeploy není potřeba |
| `sync:moderation` | netřeba (prompt/taxonomie se neměnily) |
| Env / Vercel | GDPR text na `/gdpr` až po buildu z `main` |
| GTM | — neměněno |

## Manuální testy

- Prefill → `suggest_description` v H2: **neověřeno** po Edge deployi — jeden běh na `/inzerat/novy` a `log_no` v SQL.
- Create/update: kód beze změny; neproháněno.

## Commity

- `6c93b93` — feat: log prefill description and disclose 12-month AI draft retention

## Na další session

- Smoke: Prefill 1–2 fotky → Table Editor / H2, `suggest_description` vyplněné.
- Lab DualSense Hydratovat (fixture teď dohodou, ne 100 Kč).
- GTM `ai_prefill_submit` (dopoledne).
- Až víc mezer v `docs/prefill-mezery.md` → vsuvka do prefill promptu.
