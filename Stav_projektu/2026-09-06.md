# Stav projektu — 2026-09-06

**Čas zápisu:** ~11:50  
**PRD:** v3.97

Předchozí: [`2026-09-05.md`](./2026-09-05.md).

## Hotovo v této iteraci

Vercel logy: stovky `GET /meta.json` → 404 (356 za 24 h). Není to chyba webu — Meta Ads crawler (`facebookexternalhit`) plošně sahá na nedokumentovaný `/meta.json` u reklamních URL (UTM). Schema neexistuje; soubor nepřidávat.

Catch-all `app/[slug]` bral `slug=meta.json` a renderoval plnou SSR 404. Middleware matcher `.json` nevynechává, takže každý hit šel i do `updateSession()`.

Ověření v `@supabase/auth-js` (`GoTrueClient._getUser`): anonymní request **bez** `sb-*-auth-token` cookie Auth API **nevolá**. Náklad je invocation + SSR, ne Supabase.

Oprava: `isJunkProbePath()` v `src/config/junk-probe-paths.ts`, v `src/middleware.ts` holé `404` **před** `updateSession()`. Seznam: `/meta.json`, WP/git/env sondy. Review: matchování sedí, false positive na reálné route je nulový.

## Infrastruktura

| Položka | Stav |
|---------|------|
| Nová SQL | — není |
| Edge Functions | — neměněno |
| Env / Vercel | — neměněno (stačí deploy `main`) |

Po pushi: Vercel build. Smoke: `GET https://zapikolou.cz/meta.json` → 404, prázdné tělo (ne HTML 404 stránka). Logy: edge-middleware 404, bez serverless renderu `[slug]`.

## Commity

- `a2cd141` — fix: short-circuit junk crawler paths before listing SSR
- `be1e469` — docs: PRD v3.97 junk-path middleware and session snapshot

## Na další session

- Po deployi ověřit `/meta.json` na produkci (prázdné 404)
- FB kreativa crop bug — návod v `docs/navod-na-fb-reklamu.md` (změny v working tree, **necommittováno**)
