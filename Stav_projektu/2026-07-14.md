# Stav projektu — 2026-07-14

> **Čas:** ukončení session — produkce nasazena, GTM + Search Console hotové  
> **PRD:** v3.27  
> **Branch:** `main` (po pushi synced s `origin/main`)

## Co je hotové (tato iterace)

### GTM, cookie lišta a Consent Mode
- Container **`GTM-WGLNJRNK`** v `src/config/gtm.ts` (override `NEXT_PUBLIC_GTM_ID`)
- Vlastní cookie lišta bez externího CMP (`CookieConsentBanner`, `CookieConsentProvider`)
- Consent Mode v2 přes `gtag('consent', …)` před načtením `gtm.js`
- Stránka **`/cookies`**, odkaz v patičce + **Nastavení cookies**
- CTA `data-gtm-id` připravena pro GTM click triggery (`gtm-ids.ts`)

### Google Search Console
- Property **`zapikolou.cz`** ověřená **DNS TXT**
- Sitemap `https://zapikolou.cz/sitemap.xml` odeslaná

### Opravy z testování
- Oprava formátu consent push (GTM Preview potvrzuje default denied + update granted)
- `suppressHydrationWarning` na `<html>` / `<body>` (Tag Assistant extension)
- `/cookies` přidáno do `sitemap.ts`

## Nasazení infrastruktury

| Oblast | Stav |
|--------|------|
| Vercel produkce `zapikolou.cz` | ✅ |
| GTM + cookie lišta | ✅ nasazeno |
| GA4 tag v GTM containeru | ✅ (konfigurace mimo repo) |
| Search Console + sitemap | ✅ |
| Supabase migrace (tato session) | — nebyly |

## Manuální testy

| Scénář | Výsledek |
|--------|----------|
| Cookie lišta — první návštěva | ✅ |
| „Přijmout analytiku“ → GTM consent update | ✅ (Preview) |
| Default `analytics_storage: denied` | ✅ (Preview) |
| `/cookies`, patička, sitemap | ✅ |
| Search Console DNS | ✅ |

## Záměrné chování / backlog

- Propojení Search Console ↔ GA4 (volitelné)
- FAQ `/faq` — plánováno (PRD §11.3)
- P33 — tabulka zpracovatelů EU, AI přenos (Gemini/SCC)
- Revize právních textů advokátem (`REVIZE_PRAVNI/`)

## Commity (tato session)

| Hash | Popis |
|------|--------|
| `76c126e` | feat: vlastní cookie lišta a GTM Consent Mode |
| *(tento commit)* | fix: gtag consent + docs v3.27, Metodika §14, Stav_projektu |

## Na další session

1. Sledovat indexaci ve Search Console (Pokrytí stránek)
2. GA4 reporty — ověřit page_view po souhlasu v reálném provozu
3. Backlog dle `TO-DO_Fable.md`
