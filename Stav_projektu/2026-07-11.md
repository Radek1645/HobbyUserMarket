# Stav projektu — 2026-07-11

> **Čas:** pozdní večer (třetí iterace session)  
> **PRD:** v3.24  
> **Branch:** `main` — feat `872a830`

## Co je hotové (tato iterace — P26/P27)

### Nahlášení (P27)
- Inline „Nahlásit inzerát“ na detailu (přihlášení)
- Standalone `/nahlasit` + odkaz v patičce
- Migrace **040** (standalone sloupce, rozšířené důvody), **041** (`report_no`), **042** (service_role INSERT)
- E-mail adminovi (`notifyAdminReport`, env `ADMIN_NOTIFICATION_EMAIL`)
- Oprava `report_failed` — session klient pro přihlášené, service_role pro anonymní

### God Mode (P26)
- `/mod/karantena` — blocked inzeráty + skryté komentáře
- `/mod/inzeraty` — přehled s filtry stavu
- Lišta na detailu cizího inzerátu (Zablokovat / Smazat / Obnovit / Upravit)
- Layout `/mod/*` pro `moderator` + `admin`; menu v headeru
- Oprava `block_failed` — moderátorské akce přes session (ne service_role UPDATE)

## Nasazení infrastruktury

| Oblast | Stav |
|--------|------|
| Supabase migrace **040–042** | ✅ nasazeno |
| Edge Functions | ⏸️ beze změny |
| Vercel env | `ADMIN_NOTIFICATION_EMAIL` volitelné |

## Manuální testy (tato iterace)

| Scénář | Výsledek |
|--------|----------|
| Inline nahlášení | ✅ |
| `/nahlasit` — `already_reported` / úspěch | ✅ |
| God Mode Zablokovat → `/mod/karantena` | ✅ (po opravě session) |
| Obnovit / Smazat z karantény | ✅ (vizuálně potvrzeno uživatelem) |
| 3× report → auto-block | ✅ (DB + majitel UI) |

## Záměrné chování / backlog

- Stejný uživatel nemůže nahlásit znovu (anti-brigading) — záměr
- Po obnově zůstávají historické reporty v DB; 4. uživatel může okamžitě re-block
- Po 3. nahlášení redirect na detail → **404** místo potvrzení (UX backlog)
- `audit_events` při mod akcích zatím ne (P30)

## Kumulativně dnes (dřívější iterace)

- P23 smazání účtu, P31 věk 15+, quota 038/039, H3 redirect, `/mod/uzivatele`, PRD v3.23 monetizace plán

## Na další session

- Redirect po 3× nahlášení (404 fix)
- `ADMIN_NOTIFICATION_EMAIL` v produkci + test mailu
- P8/U1 — AI technické chyby vs. rejection dialog
- P20/P22 — marketing consent
- P30 — audit log
