# Stav projektu — 2026-07-11

> **Čas:** večer (ukončení session)  
> **PRD:** v3.22  
> **Branch:** `main` — push po commitech této session

## Co je hotové

### P23 — Smazání uživatelského účtu
- Migrace **`037_delete_user_account.sql`** — `posts.user_id ON DELETE SET NULL`, RPC `prepare_user_account_deletion`, `account_deletion_events`
- Sdílený pipeline `src/lib/account/delete-user.ts` + `src/app/actions/account.ts`
- **Self-delete:** `/profil/nastaveni` — checkbox + potvrzení e-mailu, e-mail potvrzení (Resend)
- **Admin:** `/mod/uzivatele` — povinný důvod, ochrana admin účtů, grant balíčků
- Auth smazání přes Admin API; re-registrace stejným e-mailem možná

### P31 — Checkbox věku 15+ při registraci
- `RegistrationConsentFields` — povinný `consent_age`
- `registration-consents.ts` — validace e-mail signup + Google onboarding
- Text v `legal.ts` (`registrationAgeConsent`)

### Limity inzerátů (lifetime kredity)
- Migrace **`038_listing_quota.sql`**, **`039_listing_quota_lifetime.sql`**
- Tabulky `listing_packages`, `user_listing_entitlements`; `posts.listing_quota_consumed`
- Default **20** publikací zdarma (`free` balíček při registraci)
- UI: `/profil/nastaveni` (počítadlo), `/balicky-inzerce`, quota check při publikaci
- Admin grant v `/mod/uzivatele` + SQL postupy v `supabase-prikazy.md`

### God Mode (částečně)
- `/mod/uzivatele` — seznam profilů, smazání, přidělení balíčku
- Bootstrap admina/moderátora zdokumentován (`supabase-prikazy.md`, Metodika §11.1)

### UX / právní / ostatní
- Header — odkaz na nastavení účtu, vylepšené menu
- `ui-primitives.ts` + `.cursor/rules/ui-primitives.mdc`
- Právní drafty revize 1.1 (VOP, GDPR, podmínky inzerce, balíčky)
- Rozšíření kategorií v `categories.ts`, homepage browse

## Nasazení infrastruktury

| Oblast | Stav |
|--------|------|
| Supabase migrace **037–039** | ✅ nasazeno (potvrzeno uživatelem) |
| Vercel env / deploy | ✅ nastaveno (potvrzeno uživatelem) |
| Edge Functions | ⏸️ beze změny této session |
| Git push `origin/main` | ⏳ tento krok |

## Manuální testy

| Scénář | Výsledek |
|--------|----------|
| Migrace 037–039 v Supabase prod | ✅ (uživatel) |
| Vercel konfigurace | ✅ (uživatel) |
| Registrace — checkbox věku 15+ | ⏳ |
| Self-delete → odhlášení → `/login?message=account_deleted` | ⏳ |
| Re-registrace stejným e-mailem po smazání | ⏳ |
| Admin delete v `/mod/uzivatele` + audit | ⏳ |
| Quota 20/20 — blokace nové publikace | ⏳ |
| Admin grant +20 inzerátů | ⏳ |
| Build `npm run build` | ⏳ |

## Commity (tato session)

- `666e12a` — feat: smazání účtu, quota inzerátů a God Mode uživatelé
- `b64c68e` — docs: PRD v3.22, snapshot 2026-07-11 a synchronizace

## Na další session

- Manuální testy z tabulky výše (happy path + regrese)
- **P21** — Google OAuth VOP consent (částečně řešeno onboardingem, ověřit edge cases)
- **P20/P22** — marketing consent ukládání + stránka odvolání
- **P26** — `/mod/karantena`, `/mod/inzeraty`, moderátorská lišta na detailu
- **U25** — AI disclaimer při hydrataci
- Cron 90d neaktivity (PRD §5.5 pre-trigger e-mail)
- Ověřit Vercel build po pushi
