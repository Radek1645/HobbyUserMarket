# Stav projektu — 2026-09-07 (večer)

**Čas zápisu:** ~20:25  
**PRD:** v3.99

Předchozí: [`2026-09-07.md`](./2026-09-07.md) (~15:10, v3.98).

## Hotovo v této iteraci

### Prefill prompt (Edge už nasazený odpoledne)

Bazarová čeština (overal / kombinéza, ne kombinezon). Čitelný štítek → zapsat velikost, ne `Doplňte velikost:`. Smoke: Mothercare overal (overal + Mothercare; **52/40** vymyšlené). DualSense: `Doplňte kompatibilitu:` — katalog, hydratace ještě ne.

Prompt je v `9d45dbb`; Edge `suggest-listing-from-photos` + `compare-suggest-from-photos` nasazené v této session.

### Prefill lab — 2. krok Hydratovat

`/mod/prefill-lab`: po A/B **Hydratovat** volá produkční `moderate-listing` (preview, fixture použité / 100 Kč / Brno). Jiný prompt a model než Prefill. Zápis do `moderation_checks` + rate limit. Bez nové Edge funkce.

### Terén

Lokální `docs/prefill-mezery.md` (gitignore) — sběr mezer prefill + hydratace, prompt po každém záchytu neměnit.

## Infrastruktura

| Položka | Stav |
|---------|------|
| Nová SQL | — není |
| Edge Functions | prefill prompt **nasazen** (`suggest-listing-from-photos` + `compare-suggest-from-photos`); `moderate-listing` beze změny |
| Env / Vercel | lab UI = deploy `main` (tento push) |
| GTM | — neměněno |

## Manuální testy

- Prefill overal: ověřeno na webu (po Edge deployi).
- DualSense: prefill ověřen (`Doplňte kompatibilitu:`).
- Lab **Hydratovat:** v prohlížeči **neověřeno** — až po Vercel buildu.

## Commity

- (večer) feat lab hydratace + docs v3.99 — hash doplnit po commitu

## Na další session

- Smoke `/mod/prefill-lab`: DualSense → Hydratovat (dopsala PS5/PC, nebo otázka?).
- Overal: opraví hydratace 52/40?
- GTM tag `ai_prefill_submit` (zůstává z dopoledne).
- Až víc stejných mezer → jedna vsuvka do prefill promptu (katalog u jednoznačného výrobku). Čísla ze štítku zvlášť.
