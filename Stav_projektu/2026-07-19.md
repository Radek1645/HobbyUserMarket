# Stav projektu — 2026-07-19

> **Čas zápisu:** odpoledne (session GDPR / IP / vykání HP)

## Hotovo v této session

- Veřejná stránka **`/gdpr`** (`LegalDocumentPage` slug `gdpr`) — patička, sitemap, `llms.txt`
- Migrace **050** + cron `/api/cron/anonymize-inquiry-ips` (7 dní, IPv4 → `x.x.x.0`) — ověřeno RPC (`3` řádky)
- GDPR text: zpracovatelé Supabase `eu-west-1` + Vercel `dub1`; §6.2 věk = prohlášení (ne hard ověření)
- Backlog **P37–P39** (IP logy vs DB, retence inzerátů 2 měsíce vs 365 dní, newsletter bez send)
- HP copy: vykání („Přihlaste se“, „vašeho okolí“)
- God Mode editace cizího inzerátu (staff) — `updateListing` / `get-listing-for-edit` (WIP z tree)

## Infrastruktura

| Položka | Stav |
|---------|------|
| Migrace **050** (Supabase SQL Editor) | ✅ nasazeno |
| Vercel Function region → Dublin (`dub1`) | ✅ (uživatel) |
| Cron v `vercel.json` | ✅ (aktivní po deployi) |
| Edge Function `moderate-listing` | změny `build-prompt` v tree — deploy dle uživatele („infra ok“) |

## Dokumentace

- Metodika §2.1, §2.7–2.8, §9.1.3; PRD **v3.32**; TO-DO_Fable P37–P39

## Manuální testy

- RPC `anonymize_old_inquiry_ips(7)` → `3`
- Migrace 050 Success (2. pokus po timeoutu proxy)

## Na další session

- P37–P39 — sladit zbývající GDPR text s kódem (nebo naopak)
- P33 — Resend region / DPA; AI mimo EHP
- Po pushi: ověřit Vercel deploy + `/gdpr` na produkci; Google OAuth privacy policy URL = `https://zapikolou.cz/gdpr`
