# Stav projektu — 2026-08-30 (večer)

**Čas zápisu:** ~23:05  
**PRD:** v3.91

Předchozí: [`2026-08-28-vecer.md`](./2026-08-28-vecer.md) (P0 078/079, SEC-M07/M08; ověřeno 29. 8.).

## Hotovo v této iteraci

| Oblast | Stav |
|--------|------|
| Vyplněné `Doplňte X: hodnota` v hydrataci (Buddha / bronz) | ✅ **ověřeno localhost** (uživatel). Klient filtr + prompt v Edge |
| Expirace událostí o půlnoci v den konání (`080`) | ✅ **nasazeno** (SQL Editor). Karty = **Konání** |
| SEC-M02 / GO-6 — inquiry Origin + `Content-Type` | ✅ v kódu (Vercel po pushi) |
| SEC-M10 — změna hesla se stávajícím + recovery AMR | ✅ **ověřeno produkce** (smoke 30. 8.; hardening v tomto commitu) |
| GO-2 RLS B1–B5 | ✅ uzavřeno 30. 8. (REST 403 + `42501`) |

## Infrastruktura

| Položka | Stav |
|---------|------|
| SQL `080_event_expires_end_of_calendar_day.sql` | ✅ produkce (uživatel) |
| Edge `moderate-listing` (prompt vyplněných `Doplňte`) | ✅ `npx supabase functions deploy moderate-listing` (uživatel) |
| `npm run sync:moderation` | — jen ruční sync `build-prompt.ts` (1 řádek) |
| Nové Vercel/Resend env | — není třeba |
| GTM / DNS | — netýká se |

Appka (klientský filtr hydratace, 080 hint ve formuláři, inquiry Origin, změna hesla) jde na produkci až po Vercel buildu z pushi.

## Manuální testy

| Scénář | Výsledek |
|--------|----------|
| Prefill → Obsah `Doplňte materiál: bronz` → Hydratace | ✅ otázka na materiál pryč; Parametry `• Materiál: bronz`; rozměry/původ zůstanou |
| Změna hesla v `/profil/nastaveni` | ✅ produkce chtěla stávající heslo (30. 8.) |
| Událost hint „viditelný do půlnoci z … na …“ | kód; E2E na produkci po Vercel |

## Commity

- (doplní se po `git commit`)

## Mimo tento commit

- `docs/fb-ads/posty/` (vč. `koš/`), cover PNG, `docs/naklady-mesicni.md`, `docs/vibe_engineering_blocks.md` — nenecháno do gitu.

## Na další session

- Smoke hydratace `Doplňte` na **produkci** po Vercel Ready
- Smoke událost: hint půlnoci + karta **Konání** + `expires_at` o půlnoci
- SEC-M03 hlavičky
- DPA Seznam (P4)
- I6 negativní smoke fotek
