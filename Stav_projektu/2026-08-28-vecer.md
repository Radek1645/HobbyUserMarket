# Stav projektu — 2026-08-28 (odpoledne)

**Čas zápisu:** ~15:30  
**PRD:** v3.87

Ráno: [`2026-08-28.md`](./2026-08-28.md) (Mapy.cz proxy, v3.84).

## Hotovo v této iteraci

| Oblast | Stav |
|--------|------|
| P0 `posts` column SELECT — `anon` bez telefonu/GPS/`original_*` | ✅ **nasazeno + ověřeno** (078) |
| P0 fáze 2 — `authenticated` bez `location`/`original_*`; RPC `get_post_edit_private_fields` | ✅ **nasazeno + ověřeno** (079 + `/upravit`) |
| Audit 28. 8. sloučen do [`docs/SECURITY_UX_BACKLOG.md`](../docs/SECURITY_UX_BACKLOG.md); originál v `archive/audits/` | ✅ docs |
| SEC-M07 důvěryhodná IP (Vercel → XFF zprava) | ✅ **nasazeno + ověřeno** produkce 29. 8. |
| SEC-M08 mint visitor cookie, upload captcha flag, globální AI strop | ✅ **nasazeno + ověřeno** produkce 29. 8. (smoke, inzerát jde založit) |

## Infrastruktura

| Položka | Stav |
|---------|------|
| SQL `078_posts_column_select_grants.sql` | ✅ produkce (uživatel, SQL Editor) |
| SQL `079_posts_edit_private_rpc.sql` | ✅ produkce (uživatel, SQL Editor) |
| Nové SQL u M07/M08 | — žádné (reuse `073` / `increment_anonymous_rate_limit`) |
| `npm run sync:moderation` | ✅ uživatel |
| Edge `moderate-listing` | ✅ nasazeno (včetně `_shared/client-ip.ts`) |
| Edge `suggest-listing-from-photos` | ✅ nasazeno |
| Nové Vercel/Resend env | — není třeba (`ANONYMOUS_RATE_LIMIT_SALT` už je) |
| GTM / DNS | — netýká se |

**Hint `GRANT SELECT ON public.posts` v SQL Editoru nepoužívat** — znovu otevře P0.

## Manuální / automatické testy

| Scénář | Výsledek |
|--------|----------|
| 078: `anon` + `contact_phone`/`location`/`original_description` → 42501; search „teploměr“ | ✅ produkce |
| 079: `authenticated` + `slug,title` OK; `location` → 42501 | ✅ produkce |
| `/upravit` vlastního inzerátu — 404 při souběhu staré appky, po deployi špendlík + adresa | ✅ produkce |
| IP helper: spoof XFF bere pravý prvek, `cf-connecting-ip` ignorován (6 case) | ✅ localhost `tsx` |
| Guest mint / globální strop / upload captcha E2E | ❌ neproháněno na localhost (flag C typicky vypnutý) |
| Produkce po Vercel `cdc6369`/`33c411f`: smoke + založení inzerátu | ✅ 2026-08-29 (uživatel) |

## Commity

- `77647ae` — fix: keep listing GPS and original text off REST for signed-in users (078/079 app + SQL v gitu)
- `cdc6369` — fix: stop guest AI wallet drain via spoofed IP and unlimited visitor mint

## Na další session

- **SEC-M09** Turnstile v Next (`resendSignupVerificationEmail`, `/api/inquiry`)
- SEC-M10 změna hesla; SEC-M03 hlavičky
- GO-2 RLS B1–B5; GO-6 inquiry Origin
- DPA Seznam (z rána)
