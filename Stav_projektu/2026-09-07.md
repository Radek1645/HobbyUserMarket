# Stav projektu — 2026-09-07

**Čas zápisu:** ~15:10  
**PRD:** v3.98

Předchozí: [`2026-09-06.md`](./2026-09-06.md).

## Hotovo v této iteraci

### Title značka

GA (31. 8. – 6. 9.) ukazoval `Založit inzerát | HobbyUserMarket` a `Moje inzeráty | HobbyUserMarket`. Sedm `metadata.title` přepsáno na `SITE_DISPLAY_NAME`. Staff `| God Mode` beze změny. V `src/` už `HobbyUserMarket` není.

### Prefill (working tree z dřívějška, jde ve stejném pushi)

- Prompt: bazarová čeština, čitelný štítek → zapsat do nabídky (ne `Doplňte velikost:`), overal/kombinéza.
- GA4 `ai_prefill_submit` při odeslání fotek (`pushAiPrefillSubmit`).
- Metodika §6.12 H — SQL na Prefill bez publikace.
- FB ads návod + smazané 4×5 PNG; `docs/naklady-mesicni.md`; `.gitignore` `docs/prefill-kuriozity.md`.

## Infrastruktura

| Položka | Stav |
|---------|------|
| Nová SQL | — není |
| Edge Functions | prompt v `_shared/moderation/suggest-listing.ts` — **deploy TODO** (`suggest-listing-from-photos` + `compare-suggest-from-photos`) |
| Env / Vercel | — neměněno (appka = deploy `main`) |
| GTM | nový event `ai_prefill_submit` — tag v GTM konzoli zvlášť, kód jen pushne dataLayer |

## Manuální testy

Lokální `next dev`, curl:

- `/inzerat/novy` → 200, `<title>Založit inzerát \| zaPikolou.cz</title>`
- `/podminky-inzerce`, `/nahlasit` → 200, title `… \| zaPikolou.cz`
- `/moje-inzeraty`, `/profil/nastaveni`, `/auth/nastavit-heslo` → 307, title v odpovědi `… \| zaPikolou.cz`

GA historická zobrazení zůstanou pod starým názvem.

## Commity

- (doplní se po `git commit`)

## Na další session

- Deploy Edge Functions (prefill prompt)
- GTM: Custom Event `ai_prefill_submit` → GA4
- Smoke title na produkci `/inzerat/novy`
- Default OG obrázek
