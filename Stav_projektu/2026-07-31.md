# Stav projektu — 2026-07-31

**Čas:** ~23:55  
**Větev:** `main`  
**Zaměření:** Staging fotek + Sharp AI varianty (bez Supabase Image Transformations); auth dokončení e-mailu  
**Push:** ano (tento commit)

---

## Hotovo

### AI fotky — rychlost + SEC-H02

- Originály (max. 1920 px / 1 MB) se nahrají **jednou** do privátního `moderation-image-staging`
- Server Action `prepareModerationImageRenditions` přes **Sharp** uloží WebP 1024 px (Gemini) a 512 px (Sightengine) do `moderation-image-renditions` pod SHA-256
- Edge `moderate-listing` znovu stáhne originál, spočítá hash (SEC-H02) a načte varianty podle hashe — klient nemůže podvrhnout vztah originál → AI náhled
- Approval váže i `main_image_index` (067)
- Cron čistí staging + renditions po 24 h
- **Bez** placených Supabase Storage Image Transformations

### Auth

- Klientské dokončení session: `/auth/dokoncit` (PKCE code + hash), `/auth/potvrdit`
- `emailRedirectTo` → `/auth/dokoncit`; lepší copy u resend/spam
- `revalidatePath` po auth (Header / FAB)

### Docs

- PRD **v3.52**, Metodika § krok 3, `moderace-inzeratu.md`, `ukazka-moderace-hydratace.md`, `supabase-prikazy.md` § Schéma, `TO-DO-dalsi-den.md` § E

## Infrastruktura

| Položka | Stav |
|---------|------|
| SQL `067_moderation_image_staging.sql` | ✅ spuštěno (uživatel) |
| SQL `068_moderation_image_renditions.sql` | ✅ spuštěno (uživatel) |
| Edge `moderate-listing` (`sync` + deploy) | ⬜ **TODO po pushi** — uživatel nasadí |
| Env `SUPABASE_SERVICE_ROLE_KEY` | ✅ `.env.local` + Vercel Production/Preview |
| Vercel build po pushi | ⬜ po pushi |
| GTM / DNS | beze změny |

## Manuální testy

| Scénář | Výsledek |
|--------|---------|
| Lint / `tsc` / `next build` / Deno check Edge | ✅ v session |
| Publish create s fotkami (staging → Sharp → Edge → publish) | ⬜ po deployi Edge + Vercel |
| Edit bez změny fotek | ⬜ po deployi |
| Negativní: cizí path / chybějící rendition | ⬜ po deployi |

## Commity

- `fad456c` — feat: staging fotek + Sharp AI varianty a dokonceni email auth

## Backlog / Na další session

1. Deploy Edge `moderate-listing` + smoke publish s 6 fotkami
2. Security smoke § E I5/I6 (`TO-DO-dalsi-den.md`)
3. Formulář SoR — § A
4. Dětské zboží — § B
5. Počet poptávek — § C
6. Půjčovna — § D
)
