# Stav projektu — 2026-08-28

**Čas zápisu:** ~12:20  
**PRD:** v3.84

## Hotovo v této iteraci

| Oblast | Stav |
|--------|------|
| Mapy.cz volání přesunuto na server (`/api/mapy/suggest`, `/api/mapy/rgeocode`) | ✅ kód |
| `MAPY_CZ_API_KEY` místo `NEXT_PUBLIC_MAPY_CZ_API_KEY` (mimo JS bundle) | ✅ |
| Rate limit 60 suggest / 20 rgeocode / h na hashed IP (`increment_anonymous_rate_limit`) | ✅ existující RPC, bez migrace |
| Přeposlání `Origin`/`Referer` (HTTP-referrer lock na klíči) | ✅ |
| UX `LocationInput` + header Poloha beze změny | ✅ |
| B-06 v `dpa-checklist.md` — správce/zpracovatel + nový tok | ✅ text (DPA od Seznamu stále chybí) |

## Infrastruktura

| Položka | Stav |
|---------|------|
| Nové SQL migrace | — žádné (reuse `073` / `anonymous_rate_limits`) |
| Edge Functions deploy | — není třeba |
| `.env.local` `MAPY_CZ_API_KEY` | ✅ uživatel |
| Vercel `MAPY_CZ_API_KEY` + smazání `NEXT_PUBLIC_*` | ✅ uživatel (Production i Preview) |
| DPA Seznam.cz | ❌ TODO — napsat na developer.mapy.com/kontakt |

**Nasazeno / ověřeno:** kód je v gitu a jde na Vercel s pushem. Produkční smoke P3 (našeptávač na zapikolou.cz, Network bez `api.mapy.cz`) **ještě ne** — až po zeleném Vercel buildu.

## Manuální testy (localhost)

| Scénář | Výsledek |
|--------|----------|
| Header Poloha — našeptávač (Havlišova, Brno) → Použít → okolí Královo Pole | ✅ |
| Formulář inzerátu — lokalita potvrzena (Sedláčková, Líšeň) | ✅ |
| GPS / rgeocode po chybějícím klíči (503) → po env 403 (chyběl Referer) → po forwardu OK | ✅ |
| Webpack PackFileCacheStrategy warning | ignorovat, nesouvisí |

## Commity

- (doplní se po `git commit`)

## Na další session

- P3: produkční smoke Poloha po Vercel deployi
- P4: DPA Seznam (B-06)
- O4: GA4 `generate_lead` na produkci (z včerejška)
